---
import BaseHead from "../components/BaseHead.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import { useTranslations, type UiType } from "../i18n/utils";
import "@fortawesome/fontawesome-svg-core/styles.css";

type Props = {
  title: string;
  description: string;
  lang?: string;
};

const { title, description, lang } = Astro.props as Props;

const t = useTranslations(lang as UiType);
---

<!doctype html>
<html lang={lang}>
  <head>
    <BaseHead lang={lang} title={title} description={description} />
  </head>
  <body>
    <Header title={t("site.title")} lang={lang} />
    <main>
      <slot />
    </main>
    <Footer />
    <script>
      import { library, config, dom } from "@fortawesome/fontawesome-svg-core";
      import { faGgCircle, faYandex, faMedium, faYoutube, faFacebook, faInstagram } from "@fortawesome/free-brands-svg-icons";

      config.autoAddCss = false;

      library.add(faGgCircle);
      library.add(faYandex);
      library.add(faMedium);
      library.add(faYoutube);
      library.add(faFacebook);
      library.add(faInstagram);
      dom.watch();
    </script>
  </body>
</html>
